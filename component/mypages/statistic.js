import React, { useState } from 'react';
import { Text, View, Button, TextInput } from "react-native";

export default function Statistic({ navigation }) {
  return (
    <View style={{ margin: 60 }}>
      <Text style={{ fontSize: 24 }}>Statistic Page</Text>
    </View>
  );
}