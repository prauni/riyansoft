import React, { useState } from 'react';
import { Text, View, Button, Image, TextInput } from "react-native";

/*const randomImage = () => {
    return (<Text>Random Image</Text>);
}

export default randomImage;
*/

const RandomImage = () => {
  return (
    <View>

      <Text>Random Image</Text>
    </View>
  );
}

export default RandomImage;
